# Experiment Log

Единый журнал: **одна запись = один эксперимент**, сквозная нумерация `exp00…expNN`.
Исторические имена (`b01`, `b03`, `b05`, `e2`, `stack`, `exp025`) сохранены как алиасы в карточках.
Постановка задачи, структура репо и окружение — в [README.md](README.md).

## Протокол (общий для всех экспериментов)

- **Валидация**: time-CV, якоря fold_00..03 = `2025-12-03 / 12-17 / 12-31 / 2026-01-14`;
  обучение fold_00..02, холдаут **fold_03** — основной критерий лидерборда.
  Прод-якорь сабмита — fold_end (`2026-02-13`).
- **Целевая функция**: RMSE на `z = log1p(target)` (≡ RMSLE); инверсия `expm1` один раз в конце.
  Абляцией лосса подтверждён квадратичный на z: RMSE 1.69277 < Tweedie vp 1.1/1.5/1.9
  (1.69287/1.69339/1.69510) < MAE 1.80181.
- Сид 42; сравнение конфигураций — только на одном наборе фолдов; никакой информации после якоря.
- Статусы: `planned → running → done / rejected`; для подготовленных, но не обученных:
  `data ready` (датасет фичей собран), `priors ok` (калибровочные артефакты готовы), `infra`.
- Карточка: Гипотеза → Метод → Результат → Вывод → Артефакты.
- Сабмиты (~6 МБ) в гит не попадают — регенерируются скриптами из `archive/expNN/src/`.

## 🏆 Champion

**exp15 — стек v2 + OOF-калибровка: CV RMSLE(fold_03) = 1.66908.**
Регенерация сабмита: `.venv/bin/python archive/exp15/src/train_stack_v2.py`.

## Leaderboard

| #  | Дата       | Эксперимент                                      | RMSLE (fold_03)    | Статус     | Итог |
|----|------------|--------------------------------------------------|--------------------|------------|------|
| 00 | 2026-08-24 | Наивный автогресс                                | 2.19506            | done       | планка |
| 01 | 2026-08-24 | CatBoost на оконных агрегатах (56f)              | 1.70261            | done       | adopted |
| 02 | 2026-08-24 | GBDT: расширенные фичи + YoY (66f)               | 1.69277            | done       | adopted |
| 03 | —          | Hurdle в z: P(buy)×E[z\|buy]                     | —                  | priors ok  | не запускался; семейство в exp09 |
| 04 | —          | BG/NBD×Gamma-Gamma → фичи                        | —                  | data ready | блок общей таблицы |
| 05 | —          | Двухмасштабный TCN, бленд                        | —                  | planned    | — |
| 06 | —          | Калибровка уровня: z-shift × сезонный β          | —                  | priors ok  | закрыт в exp10/exp15 |
| 07 | —          | PCA дневных панелей → GBDT                       | —                  | data ready | блок общей таблицы |
| 08 | 2026-08-24 | Стек v1 LGBM+Hist+Cat+Ridge                      | 1.67103            | done       | не лучше соло; робастность |
| 09 | 2026-08-24 | Фабрика exp1000 (1006 конфигов)                  | см. results.jsonl  | infra      | прогоны не сохранены |
| 10 | 2026-08-24 | Пост-калибровка exp02: порог × изотоника *(b01)* | 1.69277 (=ref)     | done       | rejected |
| 11 | 2026-08-24 | PLSI-CAL STL календарный индекс *(b03)*          | 1.69432            | done       | rejected |
| 12 | 2026-08-24 | GMM-сегментация как фича *(b05)*                 | 1.69420            | done       | rejected |
| 13 | 2026-08-24 | Абляция фичевых блоков полной таблицы *(exp025)* | **1.67027** (соло) | done       | конфиги → стек |
| 14 | 2026-08-24 | CatBoost: серия гиперпараметров на base56        | 1.70124            | done       | без adoption |
| 15 | 2026-08-24 | Стек v2 (6 моделей) + калибровка c=−0.06         | **1.66908**        | done       | **champion** |
| 16 | 2026-08-25 | Диагностика accuracy/AUC покупателя              | —                  | not run    | скрипт готов |
| 17 | 2026-08-26 | Тренды / брошенные корзины / сезонность          | **1.66847**        | done       | соло cat > стек |
| 18 | 2026-08-24 | Декомпозиция: платформенный ряд × доля юзера     | 2.19942            | done       | rejected |
| 19 | 2026-08-25 | Датасет v3 + стек v3                             | 1.67150            | done       | без adoption |
| 20 | 2026-08-25 | Смешанный стек (9 семейств)                      | 1.68608            | done       | rejected |
| 21 | 2026-08-25 | Hurdle-ансамбль: LGBM-clf × стек v3              | 1.67616            | done       | rejected |
| 22 | 2026-08-26 | EV-калибровка hurdle на bl_base_ext_pca          | 2.229 (H1)         | done       | не для RMSLE-LB |
| 23 | 2026-08-30 | Per-user CatBoost n=3 it20 (персоналка)            | **1.62463**        | done       | LB 1.6862 overfit; blend 1.66 |
| 24 | 2026-08-30 | Per-user n=5 it25 (dense 5 anchors)                | 1.63498 (1k)       | done       | pilot -0.027 vs n3 |
| 25 | 2026-08-30 | Per-user n=3 it50 / n=5 it25 heavy 6h              | 1.638 (1k)         | planned    | 6h на 50c серваке, 85ms/user |
| 26 | 2026-08-30 | Segmented LTV + Transformer blend (PR #5)          | 1.675 (quick)      | done       | leakage-safe, ADI/CV², 26w |
| 26p| 2026-08-30 | Pilots: dense/lag/transformer diagnostics          | —                  | archive    | см. archive/exp26_pilots |

Справочные точки fold_03: zero 3.20364 · median 2.28900 · naive 2.19506.

### Сабмиты на лидерборде соревнования

| Файл | Что это | LB RMSLE | Когда |
|---|---|---|---|
| `submission_bl_base_ext_pca.csv` | бленд бейзлайнов на base+ext+pca | **1.66035** ← лучший | 2026-08-25 |
| `submission_stack_ols.csv` | стек v1 (exp08, мета OLS) | 1.66069 | 2026-08-24 |
| `submission_stack_v2*.csv` (remote) | стек v2, прогон на сервере | 1.73 ⚠️ | 2026-08-25 |
| `submission_*v3*.csv` (remote) | стек v3, прогон на сервере | 2.80 ⚠️ | 2026-08-25 |

⚠️ **Аномалия серверных прогонов**: локальные fold_03 у v2/v3 = 1.669–1.673 (полная
корреляция с локальными артефактами), но LB катастрофически хуже. Гипотезы: битые/недостроенные
фичи на сервере, перепутанный файл сабмита или рассинхрон порядка строк. Расследование в процессе
(сверка md5 `train.parquet`, fold_03 серверного прогона против локального, статистика CSV).

### Диагностика (в лидерборд не входит)

- **Блочная абляция in-sample** (exp08; одиночный CatBoost с рефитом на все 4 фолда,
  fold_03 в трейне): base(71f) 1.64862 → +ext(99f) 1.64745 → +pca(131f) 1.64487 →
  +btyd(141f) 1.64263. Все блоки дают небольшой вклад; цифры — только для сравнения блоков.
- **Соло-члены стека v2** (fold_03): cat_rmse 1.67062 · hist 1.67233 · lgbm 1.67333 ·
  xgb 1.67318; quantile-члены (q50) ≈1.771 — неконкурентны в соло, оставлены для
  стабилизации нулевой массы в ансамбле.
- **BTYD-соло**: bgnbd_e_gmv30 2.64, eb_e_gmv30 2.43 — хуже наива (ожидаемо: E[GMV] ≠ E[z]);
  ценность только как ортогональные фичи.

---

## exp00 — Наивный автогресс (baseline)

- **Гипотеза**: gmv за последние 30 дней — приемлемый предиктор gmv следующих 30 дней.
- **Метод**: таргет fold_03 (якорь 2026-01-14) как предикт, без ML.
- **Результат**: RMSLE(fold_03) = **2.19506**; zero 3.20364, median 2.28900.
- **Вывод**: нижняя планка; ML на оконных агрегатах обязателен.
- **Артефакты**: `archive/exp01/src/baselines.py`, `archive/exp01/reports/exp00_baselines.json`.

---

## exp01 — CatBoost на оконных агрегатах, серия по n_estimators

- **Гипотеза**: оконные агрегаты (gmv/searches/to_ord/to_cart за 7–90d) +
  recency/tenure/конверсии существенно побивают наив.
- **Метод**: 56 фичей батчами по 50k юзеров (NaN→0, recency=999); трейн fold_00..02,
  вал fold_03; lr=0.05, depth=8, l2_leaf_reg=3, seed=42; n_estimators ∈ {100,300,500,1000}.
- **Результат**: 100it 1.70998 · 300it 1.70534 · 500it 1.70385 · **1000it 1.70261 ← лучшая**
  (37s). Рефит на 4 фолдах: 1.68125. Топ-важность: `to_ord_sum_90d` (27%),
  `recency_to_ord_days`, `tenure_days`, `gmv_sum_90d`.
- **Вывод**: adopted — −0.49 RMSLE к наиву; заказная история важнее сырого gmv;
  кривая ещё снижается на 1000it (предел тюнинга проверен позже в exp14: ≈1.7012).
- **Артефакты**: `archive/exp01/` (src, reports, submission_exp01.csv).

---

## exp02 — GBDT на расширенных фичах + YoY-калибровка

- **Гипотеза**: поверх exp01 дают прирост интент-сигналы (EDA: градиент P(y>0) 29%→82%
  по децилям searches_14d), конверсии по источникам, декомпозиция «частота × чек», due_ratio
  и доли gmv_search/cat; сезонный YoY-индекс поправит праздничное окно сабмита.
- **Метод**: абляция блоков поверх base56: EWMA(h7/h30)+тренды; конверсии s2o/c2o/o2c;
  AOV_30, ord_days_30; due_ratio; доли+тренды gmv_search/cat; pct_rank_gmv30.
  YoY: `ŝ = GMV(14.02–15.03.25)/GMV(15.01–13.02.25)` = 1.16278, предикт `ŷ·ŝ^β`,
  β по прокси-фолду (якорь 2025-02-14 — календарный близнец сабмит-окна).
- **Результат**: база-контроль 1.70284 (parity exp01, dev 0.00023); +EWMA+тренды 1.70304
  (нейтрально); **+конверсии+декомпозиция+due_ratio+доли (66f) → 1.69277 (−0.59%)**;
  full74 хуже (1.69302–1.69545). LOFO лучшего: fold_00 1.81545 · fold_01 1.77420 ·
  fold_02 1.71090 · fold_03 1.69277 — улучшение на всех фолдах. YoY: β̂=1.0 по прокси
  (1.853→1.835; сетка монотонна до края — выбор консервативен). Рефит на 4 фолдах: 1.67165.
- **Вывод**: adopted ✓ (порог ≤1.69920). Прирост дали конверсии источников, декомпозиция
  и due_ratio; EWMA/тренды не вошли. Оговорки: β̂ — граничное решение сетки (не
  идентифицируем по прокси); перепроверено в exp11 — CV-фолдам календарная поправка не нужна.
- **Артефакты**: `archive/exp02/`; кэш фичей `data/v2/features_exp02/` (генератор
  `archive/exp02/src/exp02_features.py`) — база для exp10–16; блок `x_*` также в
  `data/v2/features_ext/`. Ранняя WIP-итерация (`train_e2.py`/`submit_e2.py`/
  `features_ext.py`, alias e2) удалена как дублирующая (история — коммит 5fbb88d).

---

## exp03 — Hurdle в z-пространстве: P(buy)×E[z|buy]  `[priors ok]`

- **Гипотеза**: `E[z|X] = P(y>0|X)·E[z|y>0,X]` — точное тождество моментов; при 46% нулей
  раздельные стадии дадут −0.5..−2%.
- **Метод**: Stage 1 — LGBM binary без scale_pos_weight на интент/рекенси-фичах;
  Stage 2 — LGBM regression на z по покупателям; изотоника p̂ по pooled OOF;
  перемножение в z; Ridge-стек поверх [hurdle, single, naive]. Adoption: ≥0.5% на 3 из 4 фолдов.
- **Результат**: end-to-end не запускался. Приоры готовы: `reports/exp03_hurdle_priors.json` —
  P(buy), E[z|buy], mean_y по сетке рекенси×интент (16 сегментов × 4 фолда);
  stage-0 baseline pred_z = p_buy·mean_z_buyers.
- **Вывод**: запускать через семейство `hurdle` фабрики exp09 (приоритет №1 там).
- **Артефакты**: `src/exp03_hurdle_priors.py`, `reports/exp03_hurdle_priors.json`.

---

## exp04 — BG/NBD×Gamma-Gamma как генератор фичей  `[data ready]`

- **Гипотеза**: вероятностная декомпозиция GMV = E[N₃₀]·E[V] даёт сигналы, ортогональные
  бустингу, особенно на холодных юзерах (0–2 заказа).
- **Метод**: MLE BG/NBD на RFM-суффиксах → MAP η_u с интент-модуляцией; чек — Gamma-Gamma
  шринкейдж; E[log1p(S)] через MC-сэмплирование (дельта-метод отклонён численной проверкой:
  ошибка 0.8–2.2 в z на хвостах).
- **Результат**: фит на pooled train-фолдах: r=1.251 α=19.2 a=0.135 b=1.238;
  GG p=0.071 q=3.02 (⚠️ p<1 ⇒ gg_e_value ≈ константа 54 — слабая фича). Соло-диагностика:
  bgnbd_e_gmv30 2.64, eb_e_gmv30 2.43 — хуже наива (ожидаемо).
- **Вывод**: как соло нет; ценность подтверждена блочной абляцией (+btyd in-sample:
  1.64487→1.64263, exp08) — блок включён в общую таблицу стека.
- **Артефакты**: `src/build_features_bgnbd.py` → `data/v2/features_bgnbd/<fold>.parquet`
  (+ fit_params.json); схема — `data/v2/MANIFEST.md`.

---

## exp05 — Двухмасштабный TCN, бленд с GBDT  `[planned]`

- **Гипотеза**: end-to-end NN на дневных рядах ловит динамику, недоступную оконным
  агрегатам; ценность — декоррелированный член ансамбля (+1–3%), не соло (~30–35% шанс).
- **Метод**: TCN ~360k параметров: 35 дней дневного + 26 недель недельного разрешения
  (T≈61 токен); ResBlocks Conv1d k=3 dil 1/2/4; календарные эмбеддинги (DoW, праздники
  23.02/08.03) + статика в голову; MSE на z; аугментация случайными якорями (τ≈120д);
  AdamW 3e-4 cosine, EMA, fp32 MPS; эпоха 1–2 мин.
- **Результат**: —
- **Вывод**: — (бленд α∈{0.2..0.5}; без улучшения — отбросить, sunk cost ≈ 40 мин)
- **Артефакты**: —

---

## exp06 — Калибровка уровня: глобальный z-shift × сезонный β  `[priors ok]`

- **Гипотеза**: платформа растёт (+46% YoY), окно теста сезонно приподнято (близнец 2025 =
  ×1.163) → модели систематически недооценивают февральско-мартовское окно; поправка уровня
  в z даст −0.5..−2% без изменения модели.
- **Метод**: поверх любого сабмита — (а) сдвиг `ẑ' = ẑ + c` по pooled OOF (изотоника/скаляр);
  (б) сезонная поправка `ẑ' = ẑ + β·log(ŝ)`, β∈[0..1]; (в) recency-взвешивание фолдов
  как альтернативный учёт дрейфа.
- **Результат**: индексы готовы (`reports/exp06_calibration.json`): s_seasonal=1.1628,
  yoy_cohort_user=1.3067, local_drift=0.8094 (⚠️ локальный даунтафт перед якорем —
  калибровку вниз тоже проверять). Проверки: exp10 — порог/изотоника выигрыша не дали;
  exp11 — на fold_03 сдвиг не нужен (s*=0.9512 → −0.0016, шум); скалярный сдвиг помог
  только внутри мета-модели стека (intercept +0.024 в exp08, c=−0.06 в exp15).
- **Вывод**: закрыт — калибровка уровня полезна как часть мета-модели стека, а не как
  пост-обработка поверх готового сабмита.
- **Артефакты**: `src/exp06_calibration_indices.py`, `reports/exp06_calibration.json`,
  инструмент `src/oof_calibrate.py`.

---

## exp07 — PCA-компоненты дневных панелей → GBDT  `[data ready]`

- **Гипотеза**: плотное представление дневного ряда (56д × log1p(gmv, searches, to_ord,
  to_cart) = 224 dims), сжатое IncrementalPCA до 32 компонент, даёт бустингу динамику
  (уровень/частота/тренд) компактнее сотен сырых лагов.
- **Метод**: стандартизация + IncrementalPCA(32); фит строго на пре-якорных окнах
  fold_00..02; fold_03/fold_end — чистый transform (утечки нет).
- **Результат**: EVR(32)=0.514; pca_00 ≈ общий уровень активности (corr с log1p gmv_90d
  = 0.63). Вклад подтверждён: in-sample 1.64745→1.64487 (exp08); честно в exp13:
  66f 1.69273 → 109f(+pca+btyd) 1.67055.
- **Вывод**: блок включён в общую таблицу стека.
- **Артефакты**: `src/build_features_ext_pca.py` → блок `pca_00..31` в
  `data/v2/features_ext/fold_*/`.

---

## exp08 — Стек v1: LGBM+HistGBR+CatBoost+Ridge, грид-поиск

- **Гипотеза**: ансамбль разнородных моделей в z с мета-моделью на честных OOF побьёт
  соло CatBoost; интерсепт мета-модели встроит глобальную калибровку уровня (exp06).
- **Метод** (`train_stack.py`): Stage A — компактные сетки на внутреннем сплите
  fold_00+01→fold_02; Stage B — OOF leave-one-fold-out по fold_00..02; Stage B2 — прод-бейзы
  на всех трёх фолдах (ES на fold_02); Stage C — RidgeCV/OLS(positive) на pooled OOF;
  fold_03 — чистый холдаут. Фичи: все блоки (141f).
- **Результат**: стек OLS **1.67103**, ridgecv 1.67105; соло: cat 1.67046 · hist 1.67230 ·
  lgbm 1.67291 · ridge 1.71487. Веса OLS: cat 0.61 / lgbm 0.27 / hist 0.13 / ridge 0,
  intercept +0.0238. Блочная абляция in-sample: base 1.64862 → +ext 1.64745 → +pca 1.64487
  → +btyd 1.64263 (`bl_blocks.json`).
- **Вывод**: к exp02 выигрыш −1.3%, но лучший соло-cat на холдауте не превзойдён — GBDT
  сильно коррелированы. Ценность стека: робастность на private, автоматическая калибровка
  уровня; публичный LB ≈1.66. Развитие → exp15 (диверсификация алгоритмами и лоссами).
- **Артефакты**: `archive/exp08/src/{train_stack,submit_baseline_blocks}.py`,
  `archive/exp08/reports/{stack_search,bl_blocks}.json`.

---

## exp09 — Фабрика exp1000: 1006 конфигов, параллельный пул  `[infra]`

- **Гипотеза**: массовый перебор приоритизированных конфигов найдёт точечные выигрыши
  дешевле ручных серий.
- **Метод**: семейства в порядке приоритета: hurdle (P(buy)×E[z|buy]) → seeds (мульти-сид)
  → pseudo (self-training на fold_end) → qmap (quantile-mapping предиктов) → core
  (модель×гиперы×лоссы×recency-веса×winsorize×фичеблоки) → blends (рекомбинация кэша
  предиктов без рефита). Протокол общий: трейн 00..02, ES на fold_02, холдаут fold_03;
  микро-сетка постобработки (c×τ×q-scale) выбирается на fold_03 одинаково для всех прогонов.
  ProcessPoolExecutor под 24c/96G RAM.
- **Результат**: инфраструктура готова; сохранённых прогонов в репо нет
  (`reports/exp1000_results.jsonl` отсутствует).
- **Вывод**: использовать для end-to-end запуска exp03 (семейство hurdle) и остальных семейств.
- **Артефакты**: `src/exp1000.py`; реестр `data/v2/exp1000/index.json`;
  результаты `reports/exp1000_results.jsonl` (append-only); кэш предиктов `data/v2/exp1000/preds/`.

---

## exp10 — Пост-калибровка exp02: zero-порог × изотоника *(alias b01)*

- **Гипотеза**: зануление предиктов ниже порога (46% нулей в таргете) и изотоническая
  калибровка z по pooled OOF дадут дешёвое улучшение поверх exp02 без переобучения.
- **Метод**: LOFO OOF CatBoost (parity exp02); сетки: вариант {raw, zero, zero_iso} ×
  пул {cv, cvp} × порог t∈[0..2.0] шаг 0.05; adoption: −0.3% mean crossfit и
  fold_03 ≤ 1.69177.
- **Результат**: оптимум выродился в t*=0 (эквивалент клипа 0.058% отрицательных z):
  mean crossfit 1.74833 = raw; fold_03 1.69277 = ref; in-sample рефит 1.67165 калибровкой
  не изменился. Все t>0 монотонно ухудшают все фолды.
- **Вывод**: rejected — модель уже откалибрована по нулям; пороговая пост-обработка вредна.
- **Артефакты**: `archive/exp10/` (src, reports, submissions/b01_submission_calibrated.csv).

---

## exp11 — PLSI-CAL STL календарный индекс *(alias b03)*

- **Гипотеза**: причинно-корректный календарный множитель (STL-тренд + годовой терм
  с контролем покрытия prior_year) улучшит exp02 там, где фиксированный β=1 — эвристика.
- **Метод**: log_M strict/assist по фолдам + histnorm-вариант; прокси-фолд (якорь
  2025-02-14) для репликации механики exp02; абляция i_ref / ii_histnorm / iii_full_strict /
  iv_ref_beta1; adoption: fold_03 ≤ 1.68938.
- **Результат**: i_ref 1.69341 (parity, dev 0.00064) · **ii_histnorm 1.69432** ·
  iii_full_strict 1.69503 · iv_ref_beta1 1.69448 — лучше ref нет. Скалярный сдвиг s*=0.9512
  даёт лишь 1.69200 (−0.0016). По децилям деградаций нет, но и выигрыша нет. Прокси
  воспроизвёл механику exp02: на праздничном окне β=1 помогает (1.837→1.823), на fold_03 нет.
- **Вывод**: rejected. CV-фолдам календарная поправка не нужна; для сабмит-окна остаётся
  простая β=1 из exp02 (уже в сабмите).
- **Артефакты**: `archive/exp11/` (src, reports с deciles/figures).

---

## exp12 — GMM-сегментация как фича *(alias b05)*

- **Гипотеза**: явный сегмент юзера (кластер поведенческого профиля) добавит сигнал сверх
  непрерывных фичей, особенно на границах режимов покупательского поведения.
- **Метод**: профиль 18 признаков (log1p-уровни + рекенси/тенюр/конверсии); выбор метода по
  silhouette/stability/min-share: GMM k=4 (silhouette 0.197, ARI≥0.5, min share ≥16%);
  выравнивание кластеров между якорями — Hungarian по пересечениям юзеров (match 0.60–0.80).
  Варианты seg_cat / seg_ohe поверх ref66.
- **Результат**: seg_cat 1.69420 · seg_ohe 1.69443 vs ref 1.69341 — хуже; важность
  сегментной фичи 0.75% (ранг 23/68); between-segment var share z ≈ 0.27 уже объяснён
  базовыми фичами.
- **Вывод**: rejected — кластеризация не добавляет информации сверх фичей профиля.
- **Артефакты**: `archive/exp12/` (src, reports с figures); профили `data/v2/b05_seg/`.

---

## exp13 — Абляция фичевых блоков полной таблицы *(alias exp025)*

- **Гипотеза**: объединение всех блоков (base + x_* + pca32 + btyd) даёт лучший соло;
  собственные новые фичи не хуже полного набора организаторов («theirs»).
- **Метод**: parity-протокол exp02 (CatBoost d8 lr05 1000it seed 42); варианты: ours66
  (=exp02), ours_pca_btyd (109f), ours_all_new (147f), theirs_all (141f); стоп-лист
  семантических дублей (x_aov_30d, x_due_ratio, x_share_gmv_*_30d — дубли base/x_*).
- **Результат**: ours66 1.69273 (parity ✓) · ours_pca_btyd 1.67055 · ours_all_new 1.67034 ·
  **theirs_all 1.67027 ← лучший соло**. Разница «наши vs их» — в шуме.
- **Вывод**: прибавку дают блоки pca+btyd (−1.3% к exp02); полная таблица — стандарт
  фичей для последующих экспериментов (использована в exp15).
- **Артефакты**: `archive/exp13/src/exp025_ablation.py`,
  `archive/exp13/reports/exp025_ablation.json`.

---

## exp14 — CatBoost: серия гиперпараметров на base56

- **Гипотеза**: тюнинг гиперпараметров CatBoost выжмет дополнительное снижение RMSLE
  поверх parity-конфига exp01.
- **Метод**: сетка по n_estimators {2000..5000}, lr {0.05, 0.03}, depth {6,8,10},
  l2_leaf_reg {3,10}, bootstrap Bernoulli(subsample 0.8), early stopping od 200–300;
  протокол exp01-parity (base56, трейн 00..02 → fold_03).
- **Результат**: parity d8_lr05_1000 1.70289 (dev 0.00028 ✓); 2000it 1.70242; 3000it
  1.70217; lr03_3000 1.70166; **d8_lr03_5000_od300 → 1.70124 ← лучшая** (4976 it, 757s);
  d6 1.70307; d10 1.70156 (2365s); l2=10 и Bernoulli — нейтрально.
- **Вывод**: весь тюнинг даёт ≤0.0017 против parity — уровень шума; depth 8 оптимален,
  дальнейшее удлинение обучения нерентабельно. В стеке (exp15) cat обучается 2000it lr05.
- **Артефакты**: `archive/exp14/src/train_ablation.py`,
  `archive/exp14/reports/train_ablation.json`.

---

## exp15 — Стек v2: 6 моделей × диверсификация лоссов + OOF-калибровка

- **Гипотеза**: v1 не превзошёл соло из-за корреляции GBDT-членов; диверсификация
  алгоритмами И лоссами (quantile-члены стабилизируют нулевую массу) плюс отдельная ступень
  калибровки на inner-OOF дадут новый лучший результат (цель — к 1.64).
- **Метод** (`train_stack_v2.py`): члены lgbm_l2, lgbm_q50, hist, cat_rmse, cat_q50,
  xgb_l2 (141f); Stage A — tune на inner-сплите; Stage B — OOF LOFO; Stage B2 — прод-бейзы;
  Stage C — мета на inner OOF: ols_pos 1.74111 < ridgecv 1.74127 < lgbm_meta 1.74184;
  Stage D — калибровочная сетка (c × τ) на inner OOF → c=−0.06, τ=0 (inner 1.71211→1.70385);
  fold_03 — чистый холдаут.
- **Результат**: соло: cat_rmse 1.67062 · hist 1.67233 · lgbm 1.67333 · xgb 1.67318 ·
  q50 ≈1.771. Стек raw 1.67101 → **стек + калибровка 1.66908 ← champion**
  (−0.19% к v1, −0.21% к лучшему соло).
- **Вывод**: adopted ✓ — калибровочная ступень на inner OOF переносится на холдаут без
  деградации; quantile-члены в ансамбле полезны, в соло нет. Направление дальше:
  hurdle/pseudo/qmap семейства фабрики (exp09) как кандидаты в члены + TCN (exp05).
- **Артефакты**: `archive/exp15/src/train_stack_v2.py`,
  `archive/exp15/reports/stack_search_v2.json`; сабмиты регенерируются скриптом.

---

## exp16 — Диагностика accuracy/AUC покупателя *(not run)*

- **Гипотеза**: RMSLE-модель должна также ранжировать покупателей; замер accuracy/AUC
  по порогам z покажет, есть ли резерв у hurdle-подхода (exp03).
- **Метод**: честные LOFO OOF (CatBoost parity exp02); accuracy бинарного предикта y>0
  на сетке порогов z∈[0..3], плюс AUC/precision/recall; референсы all-buy/all-zero/mean-z.
- **Результат**: не выполнен — отчёт `accuracy_oof.json` не сохранён.
- **Вывод**: запустить при необходимости перед hurdle-работами.
- **Артефакты**: `archive/exp16/src/exp_accuracy.py`.

---

## exp17 — Фичи трендов, брошенных корзин и сезонности *(1.66847)*

- **Гипотеза**: добавление фичей краткосрочной динамики, накопленного потенциала 
  (брошенные корзины) и макро-событий улучшит прогноз нулей и всплесков трат.
- **Метод**: отдельный скрипт на Polars для 5 новых блоков признаков; обучение 
  ансамбля (CatBoost, LightGBM, XGBoost, Hist) с флагом `--quick`.
- **Результат**: оценка на fold_03 улучшилась с 1.66908 (exp15) до 1.66847; 
  лучший скор показал `[solo] cat`, обойдя мета-модель стекинга (1.67033).
- **Вывод**: гипотеза подтвердилась, фичи дают прирост. Соло-модель пока 
  побеждает, требуется полноценный перебор параметров ансамбля на сервере.
- **Артефакты**: `src/build_features_exp17.py`, `src/train_exp17.py`, 
  `reports/stack_search_exp17.json`.

---

## exp18 — Классическая декомпозиция: платформенный ряд × доля юзера  *(alias PR exp10)*

- **Гипотеза**: GMV юзера за окно = прогноз платформенного дневного ряда (классические TS:
  Holt/ETS, Holt-Winters с недельной сезонностью, SARIMA(1,1,1)(1,0,1)[7], эмпирический
  дрейф по отношениям 30д-блоков) × доля юзера (past30/G_prev30) — сольный подход уровня наива+.
- **Метод** (`src/exp10_decomposition.py`): платформенный ряд = сумма gmv всех юзеров по дням
  (reindex до полного календаря); для каждого якоря fit строго на истории ≤ якоря, горизонт H=30;
  распределение по юзерам preserve (y_u = Ĝ·past30_u/G_prev30) и шринкейдж доли псевдо-каунтами
  K∈{5,25} по активным дням; oracle_preserve (=фактический уровень окна) как потолок семейства.
  Оценка RMSLE fold_00..03 по всем 250k юзерам. Наив воспроизвёлся до цифры EDA (2.19506 на
  fold_03) — джойны честные.
- **Результат** (RMSLE fold_03 / лучший по фолдам): все варианты 2.16–3.20 против наива
  2.19506. Лучший реальный вариант drift_ratio__preserve **2.19942** (fold_03 — хуже наива);
  oracle_preserve 2.14704 (−2.2% к наиву — потолок чистого переноса уровня). Единственный
  заметный выигрыш — fold_02 (окно с январской ямой): sarima_w__preserve **2.04114** (−8.3%
  к наиву), т.к. TS поймали спад после декабрьского пика, который carry-forward тянет вперёд.
  Шринкейдж к равномерной доле катастрофичен в RMSLE (2.7–3.2): мелкие ненулевые предикты на
  45% нулевых юзеров + размывание топ-активных (57% GMV окна). Точность платформенного уровня
  неравномерная: fold_00 недобор (25–27M при факте 27.5M), fold_01 перебор (до 32M при 24.8M).
  Для сабмит-окна TS видят аплифт +7..+11% к прошлому месяцу (22.6–23.4M vs prev30 21.0M) —
  согласуется с s_seasonal=1.1628 из exp06.
- **Вывод**: **rejected как соло**. Потолок семейства «уровень × доля» низкий: перенос уровня даёт
  ≤ −2% RMSLE и то не на всех фолдах (на fold_00 oracle хуже наива). Польза остаётся только как
  вход калибровки уровня поверх моделей (территория exp06): календарная структура окна важнее
  тренда ряда, и именно она дала весь выигрыш на fold_02. Артефакты: reports/exp10_decomposition.json,
  reports/figures/exp10_fold_*.png. В .venv добавлен statsmodels 0.14.6.

---

## exp19 — Датасет v3 (очистка фичей) + стек v3 на нём  *(alias PR exp11)*

- **Гипотеза**: удаление дублирующихся/слабых/зажатых фичей (141 → 85) снизит шум и
  переобучение базовых моделей, стек на очищенном датасете сравняется или обгонит v2 при
  обучении в ~2 раза быстрее.
- **Метод** (`src/build_dataset_v3.py`): 3 раунда голосования по схеме A–H — выкинуты
  x_*-дубликаты base (100% совпадение), слабые (corr_z ≤ .03 / 43% NaN), BTYD-входы
  (копии base), окно 60d (зажато 30d/90d, corr .92–.98), EWMA-ветка чека; PCA урезан
  32 → 16 (сигнал в 00–01); сентинел recency 999 → флаг + кап 90д; BTYD-джойн inner
  (−0.8% трейна). Итог: 85 фичей + ключи + таргет = 88 колонок, 0 NULL, без дублей ключей.
  Стек (`src/train_stack_v3.py`) — протокол exp08 на data/v3: Stage A тюнинг → B OOF LOFO →
  B2 прод-бейзы → C мета → D калибровка c×τ → E скоры fold_03 + сабмиты.
- **Результат** (fold_03): стек raw **1.67278**, калиброванный **1.67150**; соло: hist 1.67238,
  cat 1.67081, lgbm 1.67321, xgb 1.68534. Против v2 (raw 1.67101 / calib 1.66908) — чуть хуже
  локально, при этом соло-качество сохранилось, а обучение ускорилось.
- **LB**: прогон с сервера дал **2.80** — аномалия (см. таблицу сабмитов выше), расследование
  отдельно; локальные артефакты консистентны (reports/stack_search_v3.json).
- **Вывод**: очистка не дала буста (GBDT и так устойчивы к коррелированному шуму), но датасет
  полезен как быстрый/стабильный бейзлайн и как база для быстрых итераций. Артефакты:
  `data/v3/` (MANIFEST.md в репо), `reports/dataset_v3_report.json`, `reports/stack_search_v3.json`,
  сабмиты `submission_{v3_cat,v3_lgbm,stack_v3,stack_v3_calib}.csv`.

## exp20 — Смешанный стек: 9 семейств моделей на base+ext+pca (без BTYD)  *(alias PR exp12)*

- **Гипотеза**: (а) BTYD-фичи шумят — лучший LB у бленда `bl_base_ext_pca` (1.66035) без них;
  (б) разнообразие алгоритмов (не только GBDT-деревья) улучшит стек: линейные модели ловят
  другое, kNN на PCA — другую геометрию, hurdle атакует 46% нулей таргета.
- **Метод** (`src/train_stack_mixed.py`): блок фичей base+ext+pca (130 колонок, BTYD-колонки
  отфильтрованы). 9 членов: lgbm/cat/xgb/hist (нелинейности), ridge + LinearSVR (линейные на
  стандартизованных), RandomForest (устойчивость к выбросам), kNN(k=100, distance) по
  PCA-компонентам (подвыборка 120k референсов), hurdle_lin (LogisticRegression P(buy) ×
  Ridge E[y|buy]). Протокол exp08: OOF LOFO fold_00..02 → мета (RidgeCV vs OLS-positive,
  выбор на inner-val fold_02) → прод-бейзы на 00+01+02 (ES fold_02) → калибровка c×τ →
  скоры fold_03 + 2 сабмита. Всё чекпоинтится (`reports/ckpt_stack_mixed/state.npz`),
  переживает обрывы. NaN в x_* ratio-фичах заполняются нулём для линейных/knn семейств
  (деревья — нативно).
- **Результат** (fold_03): стек raw **1.69159**, калиброванный **1.68608** — хуже стеков на
  чистых деревьях (v2 1.67101, v3 1.67150). Соло: cat 1.67391, hist 1.67488, lgbm 1.67606,
  rf 1.68015 — деревья ок; слабые члены: ridge 1.72628, linsvr 1.83673, knn 1.99865,
  hurdle_lin 2.04480. Мета ols_pos (inner 1.75370), калибровка c=-0.12, τ=0.75.
- **Вывод**: **rejected**. Разнообразие алгоритмов само по себе не помогает: мета-модель
  работает только если члены сопоставимы по качеству, а линейные/kNN/hurdle-члены на этих
  данных проигрывают бустингу на 0.05–0.37 и тянут смесь вниз даже с неотрицательными
  весами. Гибрид hurdle_lin (P(buy)×E[y|buy] на линейных) тоже не взлетел — hurdle-идея
  работает только с сильными моделями внутри ступеней (см. exp03/exp1000). Рабочий состав
  стека остаётся: lgbm + cat + xgb + hist (+ квантильные варианты). Побочный сигнал: соло
  скоры сервера на 0.003–0.006 хуже локальных при том же коде — ещё одно указание на
  проблему данных на сервере (см. инцидент в таблице сабмитов). Артефакты:
  `reports/stack_search_mixed.json`, `submission_stack_mixed{,_calib}.csv`.

## exp21 — Hurdle-ансамбль: LGBM-classifier P(y>0) × стек v3 E[z|y>0]  *(alias PR exp13)*

- **Гипотеза**: на zero-inflated таргете (42–46% нулей) hurdle `ẑ = P(y>0|X)·E[z|y>0,X]`
  (точное тождество моментов при z=log1p(y)) систематически обыгрывает single-stage:
  Stage 1 не тратит ёмкость бустинга на границу режимов, Stage 2 учит чистые уровни покупателей.
- **Метод** (`src/train_stack_hurdle.py`, протокол v3): Stage 1 — LGBMClassifier(binary) без
  scale_pos_weight, изотоника p̂ по OOF (выбор raw/iso на inner); Stage 2 — члены v3
  (lgbm/lgbm_q50/hist/cat/cat_q50/xgb), обученные ТОЛЬКО на y>0 и предсказывающие все строки;
  мета (ridgecv/ols_pos/lgbm_meta) выбирается по итоговому hurdle-RMSLE на inner-slice fold_02.
  Калибровка k×c×τ и бленд-α с single-LGBM-референсом — строго на inner. Чекпоинты OOF/prod
  (`reports/ckpt_stack_hurdle/state.npz`), авто-resume. Диагностика декорреляции: corr(hurdle, single).
- **Смоук (--quick, итерации ×0.24–0.33)**: HURDLE_STACK raw **1.67149** vs single-ref 1.67190,
  стек v3 full raw 1.67278 / calib 1.67150; clf AUC fold_03 0.856; corr(hurdle,single)=0.997 —
  декорреляция слабая (бустинг неявно выучивает произведение); калибровка k/c/τ переобучается
  на inner (на fold_03 −0.7%). Полный прогон решает adoption.
- **Результат** (полный прогон на сервере, 38.9 мин, data/v3 залита байт-в-байт): HURDLE_STACK
  raw **1.67616**, calib 1.68300, blend α=0.5 calib 1.68225 — хуже стека v3 (1.67150) и single-
  референса (1.67321). clf AUC 0.856; corr(hurdle,single)=0.9916. Соло-hurdle-члены: cat 1.67519,
  hist 1.67538, lgbm 1.67669. Серверный конвейер чист: fold_03 консистентен локальному протоколу,
  порядок строк sample_submit заассерен, pred_mean=39.53.
- **Вывод**: **rejected как замена single-stage**. На полных итерациях бустинг сам выучивает
  границу режимов; p̂·ẑ₂ добавляет дисперсию построчных ошибок классификатора (смоук вводил в
  заблуждение: недообученный single отставал). Калибровка k/c/τ стабильно переобучается на
  inner-slice — в минус на обоих прогонах. Наследие: (1) submission_hurdle.csv — кандидат в
  бленд с bl_base_ext_pca; (2) p̂ и ẑ₂ по отдельности — кандидаты на фичи для одиночного стека;
  (3) серверный пайплайн с прямой заливкой датасета (минуя сборку фичей) даёт корректные
  артефакты — шаблон для будущих прогонов. Артефакты: reports/stack_search_hurdle.json (+_remote),
  submissions/submission_hurdle{,_calib,_blend_calib}.csv.

---

## exp22 — EV-калибровка мат-ожидания на bl_base_ext_pca  *(alias PR exp14)*

- **Гипотеза**: hurdle `E[y|X] = P(buy) × E[y|buy]` поверх лучшего бейзлайна (одиночный
  CatBoost, LB 1.66035) уменьшит систематическое занижение суммы выручки; пер-бинные и
  пер-сегментные поправки перенесут калибровку с inner на fold_03.
- **Метод** (`src/exp14_ev_calib.py`): Stage 1 — CatBoostClassifier P(buy), отбор по
  Gini = 2·AUC−1; Stage 2 — CatBoost RMSE log1p на покупателях (конфиг exp01);
  H1 — пер-бинная коррекция по ẑ₂ (EB-shrinkage, λ=1000); H2 — пер-сегментная
  recency × intent (λ=500). Инсайт: `expm1(p·ẑ₂) ≠ p·expm1(ẑ₂)` — умножение внутри
  экспоненты занижает выручку на −57%.
- **Результат** (fold_03): ref_single_bl RMSLE 1.674 (−57.8% выручки); base p·expm1(z₂)
  1.964 (−41.1%); H1 20 bins **2.229 (−11.7%)**; H2 segments 2.231 (−12.7%). Стабильность
  факторов fold_02 vs pooled: единый временной сдвиг ~28%, кластеризация не решает перенос.
- **Вывод**: **не для RMSLE-лидерборда** (2.23 vs чемпион 1.66). Калибровка суммы полезна
  как диагностика bias; RMSLE этих сабмитов хуже чемпиона. Сабмит pooled-рефит:
  `submissions/submission_hurdle_bl_ev.csv` (сумма ~23.1M).
- **Артефакты**: `src/exp14_ev_calib.py`, `reports/exp14_ev_calib.json`.

## Инфраструктура — удалённый сервер

- Сервер: 24c/96G RAM, доступ по ssh-ключу (порт нестандартный). Окружение: системный python
  3.12 + `pip install --user` (venv недоступен без sudo), `libgomp` распакован в `~/local-libs`
  из deb (для lightgbm/xgboost/catboost), `LD_LIBRARY_PATH` обязателен.
- Пайплайн запуска: rsync репо (без data/, кроме train.parquet) → `build_features_ext_pca.py`
  → `build_features_bgnbd.py` → стеки/фабрика. Долгие прогоны — строго в tmux.
- ⚠️ Открытый инцидент: серверные сабмиты v2/v3 дают LB 1.73/2.80 при локальных fold_03
  1.669/1.672 — чек-лист диагностики: md5 `data/train.parquet` vs локальный, полнота
  `data/v2/features_ext` (5 фолдов × батчи), fold_03 серверного отчёта vs локального,
  статистика CSV (rows/mean/порядок user_id vs sample_submit).


## Бэклог

Закрыто по ходу экспериментов:

- ✅ Абляция лосса — RMSE на z подтверждён (exp02; Tweedie/MAE хуже).
- ✅ Зоопарк GBDT и ансамбль — cat/lgbm/hist/xgb сравнены (exp08/13/15); q50-члены соло не
  помогают; мульти-якорность остаётся за фабрикой (exp09).
- ✅ Пост-обработка (зануление/изотоника поверх сабмита) — вредна/нейтральна (exp10);
  калибровка работает только внутри мета-модели стека (exp08/15).
- ◐ Холодный старт — частично закрыт EB-shrinkage фичами BTYD (exp04); отдельной проверки
  сегмента tenure=0 не было.

Открыто:

- **exp03 end-to-end** через фабрику exp09 (семейство hurdle — приоритет №1).
- **Плотные якоря** (Arm B2 wave1) — «плотное поле», отдельный перезапуск.
- **exp05 TCN** — декоррелированный член бленда.
- **pseudo/qmap/seeds** семейства фабрики — кандидаты в члены стека.

### Внешнее ревью (2026-08-25, разбор гипотез)

- ◐ **Праздники и сезоны распродаж как фичи по датам.** Частично закрыто: платформенный
  YoY-индекс в сабмите (exp02, β=1); причинная календарная поправка на CV-фолдах не нужна
  (exp11 rejected). Открыто: явные флаги праздников/распродаж (23.02, 08.03, периоды акций)
  как фичи — дёшево закрывается внутри семейства `core` фабрики exp09.
- 🆕 **Error analysis: где ошибаемся больше всего.** Системного разбора ошибок чемпиона
  (exp15) по сегментам рекенси/тенюра/уровня GMV/децилям предикта не было; точечно было
  только в exp11 (децили) и EDA. Дёшево и может задать направление следующих экспериментов —
  высокий приоритет.
- ✅ **Кластеризация клиентов** — закрыто exp12 (b05): GMM k=4 (+перебор kmeans/GMM k=4–10,
  выбор по silhouette/stability/min-share) как cat/OHE-фича хуже ref; between-segment
  дисперсия z уже покрыта непрерывными фичами.
- ◐ **Клининг данных** — почти закрыто EDA (`reports/eda_2026_08_24.md`): пропусков/дублей/
  отрицательных нет; расхождения `gmv ≠ gmv_search+gmv_cat` — 0.25% строк; «пустые» дни
  конверсии (18.2%, все с `cat=1`) интерпретированы как слабый интент-сигнал. Осталось
  решить: исключать ли расхождения split'а при построении таргета.
- ◐ **Tweedie для CatBoost/LGBM.** CatBoost закрыто (exp02): RMSE 1.69277 < Tweedie
  vp 1.1/1.5/1.9 < MAE 1.80181; quantile-q50 члены проверены в стеке (exp15). Для LGBM
  Tweedie отдельно не гонялся — микрозадача семейства `core` фабрики (exp09).
- 🆕 **Внешние ряды (курс доллара, погода).** Ряд общий для всех юзеров ⇒ это якорь-уровневая
  ковариата (глобальный сдвиг во времени), а не юзеровая фича: место ей в мета-стадии стека /
  калибровке (рядом с c и β·log(ŝ)), иначе дублирует сезонный индекс. Проверять на fold_03.

<!-- Шаблон новой записи:
## expNN — <название>  *(alias <старое имя>)*

- **Гипотеза**: ...
- **Метод**: ... (протокол: трейн fold_00..02 → вал fold_03, сид 42)
- **Результат**: ... (RMSLE fold_03 ± сравнение с ref, ключевые числа по фолдам)
- **Вывод**: adopted / rejected / next step
- **Артефакты**: код/отчёты/сабмит — пути
-->


---

## exp23 — Per-user CatBoost персоналка (n=3 it20, 47 мин)

- **Гипотеза**: глобал `1.674` знает среднего, персоналка `+20ит` на 3 окнах юзера запомнит его уровень и даст `1.62`.
- **Метод**: глобал CatBoost 500it depth8 lr0.05 на 750k rows (00..02, 130f), затем для каждого из 250k юзеров `init_model=глобал` + 20ит на его 3 строках (11.4мс/юзера). 32% юзеров с 3 одинаковыми таргетами - скип.
- **Результат**: `fold_03 RMSLE 1.62463` vs глобал 1.67446 `-0.0498` (47.7 мин на M1 Pro). LB `submission_n5_it25.csv` (n=5) 1.6862 - переобучение на январь, blend 50/50 дает 1.66.
- **Вывод**: персоналка работает на валидации, но оверфит на январскую яму, нужен бленд/регуляризация. Тяжелый вариант n=3 it50 (6ч на 50c) ожидается 1.638 на 1к.
- **Артефакты**: `src/per_user_full.py` (n=3 it20, 47 мин), `src/per_user_full_n5_it25.py` (n=5 it25, 56 мин, 1.686 LB), `reports/per_user_full.json`, `reports/per_user_full_preds.npz`, графики `reports/figures/per_user_*.png`, сабмит `~/Desktop/submission_hurdle_blend.csv` (blend 1.66).

---

## exp24 — Per-user n=5 it25 dense (пилот 1к)

- **Гипотеза**: больше окон на юзера (5 vs 3) + больше итераций (25 vs 20) даст -0.02.
- **Метод**: 5 якорей с шагом 7д (17.12,24.12,31.12,07.01,14.01) x 1k юзеров, CatBoost 500it глобал + 25ит пер-юзер.
- **Результат**: 1k RMSLE `n3_it20 1.66271 -> n5_it25 1.63498` `-0.027` mean 14.7мс.
- **Вывод**: прирост есть, фулл 250k оценивается 61 мин, LB не гонялся.
- **Артефакты**: `reports/test_n5_it25.json`, `reports/per_user_dense_pilot.json`.

---

## exp25 — Heavy 6h per-user n=3 it50 (planned, 6ч на серваке)

- **Гипотеза**: 50ит на 3 строках даст 1.638 на 1к и уложится в 6ч на 50c серваке (85мс/юзера, 5.9ч).
- **Метод**: как exp23 но `ITER_PER_USER=50`, `n=3` (00,01,02), 130f, checkpoint каждые 5k, tmux на `ssh main`.
- **Результат**: planned, пилот 1k `1.63803` mean 16.12мс (см. dense pilot).
- **Вывод**: выделен отдельно, запуск вручную.
- **Артефакты**: `archive/exp23/src/train_6h.py` (заглушка), логика в `src/per_user_full.py`.

---

## exp26 — Segmented LTV pipeline с Transformer blend (PR #5)

- **Гипотеза**: leakage-safe иерархия global→class→cluster по ADI/CV² классам спроса даст робастность на нулевых/редких юзерах; Transformer на 26 неделях ×4 канала добавит декоррелированный сигнал.
- **Метод**: `src/build_segmented_features.py` — 205 признаков из `data/train.parquet` без `v2/v3`, 26-недельные ряды, классификация `smooth/erratic/intermittent/lumpy` (ADI 1.32/CV² 0.49), `MiniBatchKMeans` до 3 кластеров внутри класса. `src/train_segmented_submit.py` — global CatBoost + class/cluster модели (автовыбор CatBoost/HistGBDT/Ridge по размеру группы), бленд на `fold_02`, honest `fold_03`, refit на `00..03→fold_end`. `src/train_weekly_transformer.py` — Transformer (d_model 64, 2 layers, 4 heads) на тензоре `(N,26,4)`, `src/blend_segmented_transformer.py` — мета-бленд classical/transformer по классам. Launchers `scripts/run_segmented_pipeline.sh` (CPU) и `run_full_segmented_ensemble.sh` (CUDA).
- **Результат**: quick-проверка classical на `fold_03` RMSLE 1.67537 (120 it), smoke 5 фолдов ×250k — 205 признаков, 0 NULL, без дублей. Full runtime Transformer без GPU не гонялся (PyTorch в отдельной зависимости).
- **Вывод**: done, влито в main как PR #5 (a2c9480). Дальше — замер на full иттерациях и сравнение с per-user.
- **Артефакты**: `src/build_segmented_features.py`, `src/train_segmented_submit.py`, `src/train_weekly_transformer.py`, `src/blend_segmented_transformer.py`, `docs/SEGMENTED_PIPELINE_RUNBOOK.md`, `requirements_segmented.txt`, `requirements_transformer.txt`.

---

## exp26_pilots — Диагностика per-user / dense / lag / transformer (архив)

- **Гипотеза**: пилоты для выбора n/итераций/архитектуры перед full-прогоном.
- **Метод**: `archive/exp26_pilots/src/` — 11 скриптов: `grid_dense_*` (n=3/5/10 dense), `grid_per_user_*` (grid depth/lr/l2/it), `lag_*` (30d/60d 4ch lags), `per_user_n7_*` (n=7 it30), `transformer_*` (90d pilot/full). 1k pilots, CatBoost/Transformer сравнение с aggregates.
- **Результат**: best grid `d4 lr0.1 l2=1 it50` 1.581 (1k, n=3), n=5 vs n=3 -0.027, n=7 24ms/юзера overfit, lag 30d RMSLE 1.86 vs agg 1.67 — lags хуже, transformer pilot 1.8 vs agg 1.67.
- **Вывод**: архив диагностики, основа exp23/24 подтверждена, lags и чистый Transformer без статики не конкурентны.
- **Артефакты**: `archive/exp26_pilots/` (src + reports/grid_per_user_hypers.json, per_user_n7_full.json).
