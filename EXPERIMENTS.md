# Experiment Log

Учёт экспериментов: одна запись = один эксперимент. Обновляем таблицу лидерборда,
детали — карточкой ниже. Пушим в git после каждого значимого результата.
Общая постановка задачи, структура репо и правила работы — в [README.md](README.md).

## Leaderboard

| # | Дата       | Эксперимент                                   | CV RMSLE (fold_03) | Статус    |
|---|------------|-----------------------------------------------|--------------------|-----------|
| 0 | 2026-08-24 | Наивный автогресс (gmv за 30д)                | 2.19506            | done      |
| 1 | 2026-08-24 | CatBoost на оконных агрегатах                 | 1.70261            | done      |
| 2 | 2026-08-24 | GBDT: расширенные фичи (EWMA, тренды, YoY)    | —                  | planned   |
| 3 | 2026-08-24 | Hurdle в z-пространстве: P(buy)×E[z\|buy]     | —                  | planned   |
| 4 | 2026-08-24 | BG/NBD×Gamma-Gamma → фичи для бустинга        | —                  | planned   |
| 5 | 2026-08-24 | Двухмасштабный TCN, бленд с GBDT              | —                  | planned   |

Статусы: `planned → running → done / rejected`
Метрика: RMSLE на log1p, валидация — time-CV фолды из бейзлайна (якорь 2026-01-14 основной).
Порядок выполнения плановых: **2 → 3 → 4 → 5** (по соотношению ожидаемый эффект / затраты).

### Бэклог — нераспределённые гипотезы (обзор от 2026-08-24)

Не вошли в карточки эксп. 2–5, проверить по ходу дела:

- **Абляция лосса**: log1p+RMSE vs Tweedie (variance_power 1.1–1.9) vs MAE на одинаковых фичах — дёшево, закрывается внутри эксп. 2.
- **Зоопарк GBDT и ансамбль**: CatBoost vs LightGBM vs XGBoost на одних фичах; бленд/стекинг с наивом в z-пространстве. Мульти-якорность: одна модель на все фолды vs по фолдам.
- **Постобработка**: зануление предиктов ниже порога (доля нулей в таргете велика), глобальная калибровка уровня в z-пространстве по OOF (изотоника/коэффициент) — применяется поверх любого лучшего сабмита.
- **Холодный старт**: сегмент юзеров без истории до якоря (tenure=0) — проверить, что модель не систематически ошибается на них; при необходимости отдельная шринкейдж-логика (частично закрывается эксп. 4).

### Общие решения обзора гипотез (2026-08-24)

- Обучение — квадратичный лосс на `z = log1p(target)`: RMSLE ≡ RMSE в z-пространстве,
  оптимален предикт `E[z|X]`; инверсия `expm1` один раз в конце.
- Календарь обязателен: окно предсказания [2026-02-14..03-15] содержит 23.02 и 08.03;
  идентичное календарное окно есть в трейне ([2025-02-14..03-15]) → YoY-индекс сезонности.
- Рекенси/интент-сигналы (поиски, корзины без заказа) недоиспользованы; в exp01 топ-важность
  уже у заказной истории (`to_ord_sum_90d`, recency) — интент-каналы следующие кандидаты.
- Итоговая архитектура — стек: GBDT (эксп. 2+3) + вероятностные фичи (эксп. 4) + NN в бленде (эксп. 5).

---

## Эксперимент 0 — Наивный автогресс (baseline)

- **Гипотеза**: gmv пользователя за последние 30 дней — приемлемый предиктор gmv следующих 30 дней.
- **Метод**: таргет последнего CV-фолда (якорь 2026-01-14) как предикт, без ML.
- **Результат**: RMSLE(fold_03) = **2.19506**. Референсные точки: нулевой предикт — 3.20364, медианный предикт — 2.28900.
- **Вывод**: наив-автогресс заметно лучше нулей/медианы, но это лишь нижняя планка. Идём дальше — ML на оконных агрегатах.

---

## Эксперимент 1 — CatBoost на оконных агрегатах, серия по n_estimators

- **Гипотеза**: оконные агрегаты активности (gmv/searches/to_ord/to_cart за 7/30/60/90d) + recency/tenure/конверсии позволяют CatBoost существенно побить наив.
- **Метод**: фичи строятся батчами по 50k юзеров на якоре (`archive/exp01/src/features.py`, 56 фичей, NaN→0, recency-999). Трейн fold_00+01+02 (750k), валидация fold_03 (250k). Таргет log1p, лосс RMSE. lr=0.05, depth=8, l2_leaf_reg=3, seed=42; серия n_estimators ∈ {100, 300, 500, 1000}.
- **Результат** (RMSLE fold_03 / время обучения):
  - 100 iters: 1.70998 / 4.2s
  - 300 iters: 1.70534 / 11.0s
  - 500 iters: 1.70385 / 17.7s
  - **1000 iters: 1.70261 / 37.1s ← лучшая**
  - Финальная модель на всех 4 фолдах (1M строк): RMSLE на fold_03 = 1.68125 (рефит, больше данных), сабмит `submissions/submission_exp01.csv` на fold_end (2026-02-13).
- **Вывод**: CatBoost побил наив на ~0.49 RMSLE (2.195 → 1.703). Кривая валидации ещё медленно снижается на 1000 итерациях — можно поднять лимит итераций/lr-расписание. Топ-важность: `to_ord_sum_90d` (27%), `recency_to_ord_days`, `tenure_days`, `gmv_sum_90d` — заказная история важнее сырого gmv. Дальше: больше фичей (тренды окон, категории search/cat, gmv_search/gmv_cat), тюнинг гиперпараметров, quantile/huber-лоссы, ансамбли по якорям.

---

## Эксперимент 2 — GBDT на расширенных фичах (EWMA, тренды, декомпозиция, YoY-сезонность)

- **Гипотеза**: поверх базовых оконных агрегатов exp01 следующий шаг дают: рекенси-взвешенные уровни (EWMA), тренды/моментум, декомпозиция «частота × чек» и сезонный YoY-индекс (окно теста = праздничный сезон, близнец окна есть в трейне).
- **Метод**: к текущим 56 фичам добавить — EWMA gmv/to_ord с полураспадом {7, 30}д; тренды-отношения `gmv_7/gmv_30`, `gmv_30/gmv_90`, наклон OLS по log1p(дневной gmv) за 60д; конверсии по источникам `search_to_ord/search`, `cat_to_ord/cat`, `to_ord/to_cart` (NaN → CatBoost native); декомпозиция `AOV_30 = gmv_30/max(to_ord_30,1)` × частота; `due_ratio = days_since_last_ord / медианная_пауза юзера`; доли `gmv_search/gmv` и `gmv_cat/gmv` + их тренды; перцентильный ранг gmv_30 в популяции якоря. Сезонный YoY: платформенный индекс `ŝ = GMV(14.02–15.03.25)/GMV(смежные 30д)` → финальный предикт `ŷ·ŝ^β`, β∈[0,1] по фолдам с праздниками. Бленд предиктов якорей в z-пространстве.
- **Результат**: —
- **Вывод**: —

---

## Эксперимент 3 — Hurdle в z-пространстве: P(buy)×E[z|buy]

- **Гипотеза**: `E[z|X] = P(y>0|X)·E[z|y>0,X]`, z=log1p(y) — точное тождество моментов (проверено симуляцией); при большой доле нулей single-модель тратит ёмкость сплитов на границу режимов — раздельные стадии дадут −0.5..−2%.
- **Метод**: Stage 1 — LGBM binary **без scale_pos_weight** (ломает калибровку) на интент/рекенси-фичах (searches_7/14, корзины без заказа, текущая пауза vs медианной паузы юзера); Stage 2 — LGBM regression на z только по покупателям (частота заказов, CV чека, p90 дневного gmv). Isotonic-калибровка p̂ по pooled OOF. Перемножение в z-пространстве, expm1 в конце. Ridge-стек поверх [hurdle, single, naive] закрывает шринкейдж стадий.
- **Критерий adoption**: ≥0.5% выигрыша на 3 из 4 фолдов против single, иначе — просто член стека.
- **Результат**: —
- **Вывод**: —

---

## Эксперимент 4 — BG/NBD×Gamma-Gamma как генератор фичей для бустинга

- **Гипотеза**: вероятностная декомпозиция `GMV = E[N₃₀]·E[V]` даёт сигналы, ортогональные бустингу, особенно на холодных юзерах (0–2 заказа в истории): там работает только шринкейдж к популяции.
- **Метод**: MLE BG/NBD (r,α,s,β,p) на RFM-суффиксах всех юзеров → совместный MAP η_u + глобальные θ с интент-модуляцией `λ_u(d)=exp(η_u+θᵀz_u(d))` (поиски/корзины в лог-rate), torch/scipy без MCMC. Чек — Gamma-Gamma шринкейдж `(Σgmv+κm̄)/(n_ord+κ)`. ⚠️ Дельта-метод `log1p(μ)−Var/(2(1+μ)²)` отклонён численной проверкой: систематическая ошибка 0.8–2.2 в z на тяжёлых хвостах → `E[log1p(S)]` считать MC-сэмплированием (500 draws × 250k юзеров, секунды). Экспорт E[N₃₀], P(alive), E[V], sd как фичей в GBDT; сегментный вес ↑ при n_orders≤3.
- **Роль**: соло ~20–30% шанс; как генератор фичей ~60–70% улучшения стека. Затраты <20 мин/фолд, RAM <2 ГБ.
- **Результат**: —
- **Вывод**: —

---

## Эксперимент 5 — Двухмасштабный TCN, бленд с GBDT

- **Гипотеза**: end-to-end NN на дневных рядах юзера ловит динамику, недоступную оконным агрегатам; ценность — декоррелированный член ансамбля (+1–3%), а не соло-победа (~30–35% шанс).
- **Метод**: TCN ~360k параметров: последние 35 дней дневного разрешения + 26 недель недельного (эффективная T≈61 токен); ResBlocks Conv1d k=3 dil 1/2/4; календарные эмбеддинги на токен (DoW, день года, праздники 23.02/08.03) + статика в голову (lifetime-gmv, тенюр, средний интервал). Лосс MSE на z суммарного 30д-gmv. Аугментация случайными якорями с recency-bias (τ≈120д) → ~1.2M окон/эпоху. AdamW lr 3e-4 cosine, EMA, fp32 MPS; эпоха 1–2 мин, цикл 25–40 мин. Плотные матрицы [250k, 410, ~12] fp16 ≈ 2.5 ГБ — влезает.
- **Роль**: бленд `expm1(α·ẑ_NN+(1−α)·ẑ_LGBM)`, α∈{0.2..0.5} сеткой; если улучшения нет — отбросить без сожалений (sunk cost ≈ 40 мин).
- **Результат**: —
- **Вывод**: —

<!-- Шаблон новой записи:
## Эксперимент N — <название>
- **Гипотеза**: ...
- **Метод**: ...
- **Результат**: ... (CV метрика по фолдам, время обучения)
- **Вывод**: ... (идём дальше / откатываем)
-->
