# HANDOFF — статус проекта на 2026-08-24

Документ для быстрого ввода коллеги в курс: что сделано, что известно о данных, где лежат
артефакты и что делать дальше. Собран из трёх источников истины (актуальные версии — они):

- `EXPERIMENTS.md` — журнал экспериментов: лидерборд + карточки;
- `reports/eda_2026_08_24.md` — EDA сырых данных;
- `data/v2/MANIFEST.md` — схема фичевых датасетов для обучения.

## TL;DR

- **Задача**: предсказать суммарный GMV каждого из 250k юзеров за окно 2026-02-14 → 2026-03-15;
  метрика RMSLE, обучение в z = log1p(y) квадратичным лоссом, инверсия expm1 в конце.
- **Валидация**: time-CV, якоря fold_00..03; основной критерий — RMSLE на fold_03
  (якорь 2026-01-14). Прод-якорь для сабмита — fold_end (2026-02-13).
- **Сделано**: наив 2.19506 → CatBoost exp01 **1.70261** (лучший соло) → стек exp08
  LGBM+Hist+Cat+Ridge **1.67103** (RidgeCV/OLS на честных OOF, веса cat 0.61 / lgbm 0.27 /
  hist 0.13 / ridge 0). Фичевые датасеты собраны: base(70) + x_*(28) + pca32 + btyd(9)
  = 141 колонка (см. раздел 3); приоры калибровки (exp06) и hurdle (exp03) готовы.
- **Диагностика блоков** (in-sample, fold_03 в трейне — не для лидерборда): base 1.649 →
  +ext 1.647 → +pca 1.645 → +btyd 1.643 — все блоки дают небольшой вклад.
- **Следующие шаги по плану**: калибровка уровня/пороги поверх стека (exp06-сетка) →
  hurdle-сборка (exp03) → опционально TCN (exp05).
- **Окружение**: `.venv` (Python 3.12, polars/sklearn/catboost/lightgbm), скрипты запускать
  из корня репо: `.venv/bin/python src/<скрипт>.py`.
- ⚠️ CSV-сабмиты (~6 МБ каждый) в гит не попадают (`.gitignore`) — перегенерируются скриптами
  `src/submit_*.py`; текущие лежат локально в `submissions/`.
  Не отправлять `submission_bl_ridge.csv` — артефактно тяжёлохвостный.

---


---

# Часть 1. Журнал экспериментов (EXPERIMENTS.md)

# Experiment Log

Учёт экспериментов: одна запись = один эксперимент. Обновляем таблицу лидерборда,
детали — карточкой ниже. Пушим в git после каждого значимого результата.

## Leaderboard

| # | Дата       | Эксперимент                                   | CV RMSLE (fold_03) | Статус    |
|---|------------|-----------------------------------------------|--------------------|-----------|
| 0 | 2026-08-24 | Наивный автогресс (gmv за 30д)                | 2.19506            | done      |
| 1 | 2026-08-24 | CatBoost на оконных агрегатах                 | 1.70261            | done      |
| 2 | 2026-08-24 | GBDT: расширенные фичи (интент, EWMA, тренды) | —                  | data ready|
| 7 | 2026-08-24 | PCA-компоненты дневных панелей → GBDT         | —                  | data ready|
| 4 | 2026-08-24 | BG/NBD×Gamma-Gamma → фичи для бустинга        | —                  | data ready|
| 6 | 2026-08-24 | Калибровка уровня: OOF z-shift × сезонный β   | —                  | priors ok |
| 3 | 2026-08-24 | Hurdle в z-пространстве: P(buy)×E[z\|buy]     | —                  | priors ok |
| 8 | 2026-08-24 | Стекинг LGBM+Hist+Cat+Ridge, грид-поиск       | 1.67103            | done      |
| 5 | 2026-08-24 | Двухмасштабный TCN, бленд с GBDT              | —                  | planned   |

Статусы: `planned → running → done / rejected`; `data ready` = датасет фичей собран
(см. `data/v2/MANIFEST.md`), обучение — следующий этап; `priors ok` = калибровочные
артефакты готовы. Метрика: RMSLE на log1p, валидация — time-CV фолды (якорь fold_03 основной).
Порядок обучения следующего этапа: **базовая таблица (2+7+4 фичи вместе) → калибровка (6)
→ hurdle-сборка (3)**; TCN (5) — опционально после.

### Бэклог — нераспределённые гипотезы (обзор от 2026-08-24)

Не вошли в карточки эксп. 2–5, проверить по ходу дела:

- **Абляция лосса**: log1p+RMSE vs Tweedie (variance_power 1.1–1.9) vs MAE на одинаковых фичах — дёшево, закрывается внутри эксп. 2.
- **Зоопарк GBDT и ансамбль**: CatBoost vs LightGBM vs XGBoost на одних фичах; бленд/стекинг с наивом в z-пространстве. Мульти-якорность: одна модель на все фолды vs по фолдам.
- **Постобработка**: зануление предиктов ниже порога (доля нулей в таргете велика), глобальная калибровка уровня в z-пространстве по OOF (изотоника/коэффициент) — применяется поверх любого лучшего сабмита.
- **Холодный старт**: сегмент юзеров без истории до якоря (tenure=0) — проверить, что модель не систематически ошибается на них; при необходимости отдельная шринкейдж-логика (частично закрывается эксп. 4).

### Общие решения обзора гипотез (2026-08-24)

- Обучение — квадратичный лосс на `z = log1p(target)`: RMSLE ≡ RMSE в z-пространстве,
  оптимален предикт `E[z|X]`; инверсия `expm1` один раз в конце.
- Календарь обязателен: окно предсказания [2026-02-14..03-15] содержит 23.02 и 08.03;
  идентичное календарное окно есть в трейне ([2025-02-14..03-15]) → YoY-индекс сезонности.
- Рекенси/интент-сигналы (поиски, корзины без заказа) недоиспользованы; в exp01 топ-важность
  уже у заказной истории (`to_ord_sum_90d`, recency) — интент-каналы следующие кандидаты.
- Итоговая архитектура — стек: GBDT (эксп. 2+3) + вероятностные фичи (эксп. 4) + NN в бленде (эксп. 5).

### EDA данных (2026-08-24, `src/eda.py` → `reports/eda_2026_08_24.md`)

Целостность: 30.63M строк × 18 колонок, 250k юзеров, 409 дней, пропусков/дубликатов нет.
Плотность рядов 30%; 18.2% строк — «пустые» дни конверсии, из них 100% с `cat=1` (визит каталога
без действий) → это отдельный слабый интент-сигнал. Расхождения `gmv ≠ gmv_search+gmv_cat` —
0.25% строк (не мешает). Тождества по заказам/корзинам сходятся, отрицательных значений нет.

Таргет (fold_00..03): доля нулей 42–46%, `mean z ≈ 2.2–2.5`, `sd(z) ≈ 2.3`; skew(z)=0.35,
kurtosis=1.66 — в log-пространстве форма почти гауссова, тяжёлого хвоста нет (`y>10k` ≈ 0%).
Наив стабилен по якорям (RMSLE 2.195–2.227), corr(z_past, z_fut) ≈ 0.56 — потолок персистентности.

Сегменты по рекенси **заказа** на fold_03 (ключевое для расстановки усилий):

| Сегмент | юзеров | доля GMV окна | zeros | naive RMSLE |
|---|---|---|---|---|
| заказ <7д | 25.6% | 57.0% | 16.3% | 2.097 |
| 8–30д | 31.5% | 31.6% | 33.6% | 2.528 |
| 31–90д | 18.1% | 8.3% | 56.3% | 2.550 |
| >180д / никогда | 17.9% | 1.6% | 88.6% | 1.211 |

89% GMV окна создают юзеры с заказом за последние 30 дней; спящие почти не влияют на метрику.
Полностью «холодных» юзеров у якоря сабмита нет: 0% без активности ≥30д (но 45.9% без заказа ≥30д).

Интент-сигналы сильные: среди не покупавших 14д P(y>0)=41.8% при наличии поисков/корзины против
26.3% без; градиент по децилям `searches_14d`: P(y>0) 29%→82%, mean y 27→195.

Календарь и дрейф: платформа растёт (+46% YoY окну к окну), но у одной когорты на юзера +30.7%
целиком за счёт частоты активности (9.3→12.2 дней), чек/GMV-день плоский (8.1→8.1). Близнецовое
окно 2025 = ×1.163 к предыдущему месяцу; всплески вокруг 23.02/01.03/06.03 умеренные (×1.1–1.3);
DoW-сезонность слабая (±3%). Топ-1% юзеров держат 14.9% GMV.

Выводы для плановых экспериментов: (1) интент-фичи подтверждены — поднять приоритет внутри exp02;
(2) новый дешёвый эксп калибровки уровня (глобальная z-калибровка по OOF × сезонный β-индекс) —
вставить сразу после exp02; (3) hurdle (exp03) подтверждён структурой нулей; (4) рост платформы —
за счёт частоты ⇒ EWMA/частотные фичи полезнее AOV-декомпозиции.

---

## Эксперимент 0 — Наивный автогресс (baseline)

- **Гипотеза**: gmv пользователя за последние 30 дней — приемлемый предиктор gmv следующих 30 дней.
- **Метод**: таргет последнего CV-фолда (якорь 2026-01-14) как предикт, без ML.
- **Результат**: RMSLE(fold_03) = **2.19506**. Референсные точки: нулевой предикт — 3.20364, медианный предикт — 2.28900.
- **Вывод**: наив-автогресс заметно лучше нулей/медианы, но это лишь нижняя планка. Идём дальше — ML на оконных агрегатах.

---

## Эксперимент 1 — CatBoost на оконных агрегатах, серия по n_estimators

- **Гипотеза**: оконные агрегаты активности (gmv/searches/to_ord/to_cart за 7/30/60/90d) + recency/tenure/конверсии позволяют CatBoost существенно побить наив.
- **Метод**: фичи строятся батчами по 50k юзеров на якоре (`archive/exp01/src/features.py`, 56 фичей, NaN→0, recency-999). Трейн fold_00+01+02 (750k), валидация fold_03 (250k). Таргет log1p, лосс RMSE. lr=0.05, depth=8, l2_leaf_reg=3, seed=42; серия n_estimators ∈ {100, 300, 500, 1000}.
- **Результат** (RMSLE fold_03 / время обучения):
  - 100 iters: 1.70998 / 4.2s
  - 300 iters: 1.70534 / 11.0s
  - 500 iters: 1.70385 / 17.7s
  - **1000 iters: 1.70261 / 37.1s ← лучшая**
  - Финальная модель на всех 4 фолдах (1M строк): RMSLE на fold_03 = 1.68125 (рефит, больше данных), сабмит `submissions/submission_exp01.csv` на fold_end (2026-02-13).
- **Вывод**: CatBoost побил наив на ~0.49 RMSLE (2.195 → 1.703). Кривая валидации ещё медленно снижается на 1000 итерациях — можно поднять лимит итераций/lr-расписание. Топ-важность: `to_ord_sum_90d` (27%), `recency_to_ord_days`, `tenure_days`, `gmv_sum_90d` — заказная история важнее сырого gmv. Дальше: больше фичей (тренды окон, категории search/cat, gmv_search/gmv_cat), тюнинг гиперпараметров, quantile/huber-лоссы, ансамбли по якорям.

---

## Эксперимент 2 — GBDT на расширенных фичах (EWMA, тренды, декомпозиция, YoY-сезонность)

- **Гипотеза**: поверх базовых оконных агрегатов exp01 следующий шаг дают: интент-сигналы
  (поиски/корзины без заказа — EDA: градиент P(y>0) 29%→82% по децилям searches_14d),
  рекенси-взвешенные уровни (EWMA), тренды/моментум, частотные фичи (EDA: рост платформы целиком
  за счёт частоты активности, чек плоский) и сезонный YoY-индекс.
- **Метод**: к текущим 56 фичам добавить — EWMA gmv/to_ord с полураспадом {7, 30}д; тренды-отношения `gmv_7/gmv_30`, `gmv_30/gmv_90`, наклон OLS по log1p(дневной gmv) за 60д; конверсии по источникам `search_to_ord/search`, `cat_to_ord/cat`, `to_ord/to_cart` (NaN → CatBoost native); декомпозиция `AOV_30 = gmv_30/max(to_ord_30,1)` × частота; `due_ratio = days_since_last_ord / медианная_пауза юзера`; доли `gmv_search/gmv` и `gmv_cat/gmv` + их тренды; перцентильный ранг gmv_30 в популяции якоря. Сезонный YoY: платформенный индекс `ŝ = GMV(14.02–15.03.25)/GMV(смежные 30д)` → финальный предикт `ŷ·ŝ^β`, β∈[0,1] по фолдам с праздниками. Бленд предиктов якорей в z-пространстве.
- **Артефакты данных (готово)**: блок `x_*` (28 фичей) в `data/v2/features_ext/fold_*/`
  (`src/build_features_ext_pca.py`), NaN-политика и схема — `data/v2/MANIFEST.md`.
- **Результат**: —
- **Вывод**: —

---

## Эксперимент 7 — PCA-компоненты дневных панелей → GBDT

- **Гипотеза**: плотное представление дневного ряда юзера (56д × log1p(gmv, searches, to_ord,
  to_cart) = 224 dims), сжатое IncrementalPCA до 32 компонент, даёт бустингу динамику
  (уровень/частоту/тренд) компактнее сотен сырых лагов; компоненты ортогональны оконным агрегатам.
- **Метод**: стандартизация + IncrementalPCA(32), фит строго на пре-якорных окнах train-фолдов
  fold_00..02, fold_03/fold_end — чистый transform. EVR(32) = 0.514; pca_00 ≈ общий уровень
  активности (corr с log1p gmv_90d = 0.63).
- **Артефакты данных (готово)**: блок `pca_00..31` в `data/v2/features_ext/fold_*/`.
- **Результат**: —
- **Вывод**: —

---

## Эксперимент 6 — Калибровка уровня: глобальный z-shift по OOF × сезонный β-индекс

- **Гипотеза**: платформа растёт (+46% YoY, у когорты +30.7% на юзера за счёт частоты), а окно
  теста сезонно приподнято (близнец 2025 = ×1.163 к январю) → модели, обученные на декабрьско-
  январских фолдах, систематически недооценивают февральско-мартовское окно; поправка уровня в
  z-пространстве даст дешёвые −0.5..−2% RMSLE без изменения модели.
- **Метод**: поверх лучшего сабмита — (а) глобальная калибровка `ẑ' = ẑ + c`, c подбирается по
  pooled OOF всех фолдов (изотоника/скаляр); (б) сезонная поправка `ẑ' = ẑ + β·log(ŝ)` с
  платформенным индексом `ŝ` из близнецового окна 2025, β∈[0..1] сеткой по фолдам с праздниками;
  (в) сравнить с recency-взвешиванием фолдов при обучении как альтернативным способом учёта дрейфа.
- **Критерий adoption**: улучшение на fold_03 и минимум на 2 из 4 фолдов.
- **Артефакты (готово)**: reports/exp06_calibration.json — s_seasonal=1.1628,
  yoy_cohort_user=1.3067, local_drift=0.8094 (локальный даунтафт перед якорем: декабрьский
  пик нормализуется ⇒ калибровку вниз тоже проверять), сетки c/β внутри.
- **Результат**: —
- **Вывод**: —

---

## Эксперимент 3 — Hurdle в z-пространстве: P(buy)×E[z|buy]

- **Гипотеза**: `E[z|X] = P(y>0|X)·E[z|y>0,X]`, z=log1p(y) — точное тождество моментов (проверено симуляцией); при большой доле нулей single-модель тратит ёмкость сплитов на границу режимов — раздельные стадии дадут −0.5..−2%.
- **Метод**: Stage 1 — LGBM binary **без scale_pos_weight** (ломает калибровку) на интент/рекенси-фичах (searches_7/14, корзины без заказа, текущая пауза vs медианной паузы юзера); Stage 2 — LGBM regression на z только по покупателям (частота заказов, CV чека, p90 дневного gmv). Isotonic-калибровка p̂ по pooled OOF. Перемножение в z-пространстве, expm1 в конце. Ridge-стек поверх [hurdle, single, naive] закрывает шринкейдж стадий.
- **Критерий adoption**: ≥0.5% выигрыша на 3 из 4 фолдов против single, иначе — просто член стека.
- **Артефакты данных (готово)**: reports/exp03_hurdle_priors.json — эмпирические P(buy),
  E[z|buy], mean_y по сетке рекенси заказа × интент (16 сегментов × 4 фолда); stage-0 baseline:
  pred_z = p_buy·mean_z_buyers сегмента.
- **Результат**: —
- **Вывод**: —

---

## Эксперимент 4 — BG/NBD×Gamma-Gamma как генератор фичей для бустинга

- **Гипотеза**: вероятностная декомпозиция `GMV = E[N₃₀]·E[V]` даёт сигналы, ортогональные бустингу, особенно на холодных юзерах (0–2 заказа в истории): там работает только шринкейдж к популяции.
- **Метод**: MLE BG/NBD (r,α,s,β,p) на RFM-суффиксах всех юзеров → совместный MAP η_u + глобальные θ с интент-модуляцией `λ_u(d)=exp(η_u+θᵀz_u(d))` (поиски/корзины в лог-rate), torch/scipy без MCMC. Чек — Gamma-Gamma шринкейдж `(Σgmv+κm̄)/(n_ord+κ)`. ⚠️ Дельта-метод `log1p(μ)−Var/(2(1+μ)²)` отклонён численной проверкой: систематическая ошибка 0.8–2.2 в z на тяжёлых хвостах → `E[log1p(S)]` считать MC-сэмплированием (500 draws × 250k юзеров, секунды). Экспорт E[N₃₀], P(alive), E[V], sd как фичей в GBDT; сегментный вес ↑ при n_orders≤3.
- **Роль**: соло ~20–30% шанс; как генератор фичей ~60–70% улучшения стека.
- **Артефакты данных (готово)**: `data/v2/features_bgnbd/<fold>.parquet` (+ fit_params.json):
  p_alive, en30 (hyp2f1), eb_lambda_n30 (EB Gamma-Poisson, холодостойкий), gg_e_value,
  произведения на чек. Фит на pooled train-фолдах: r=1.251 α=19.2 a=0.135 b=1.238;
  GG p=0.071 q=3.02 v=1546 (⚠️ p<1 ⇒ gg_e_value почти константа — слабая фича).
  Соло на fold_03: bgnbd_e_gmv30 RMSLE 2.64, eb_e_gmv30 2.43 (хуже наива в z-пространстве —
  ожидаемо: E[GMV] ≠ E[z]; ценность — как ортогональные фичи + шринкейдж холодных).
- **Результат**: —
- **Вывод**: —

---

## Эксперимент 5 — Двухмасштабный TCN, бленд с GBDT

- **Гипотеза**: end-to-end NN на дневных рядах юзера ловит динамику, недоступную оконным агрегатам; ценность — декоррелированный член ансамбля (+1–3%), а не соло-победа (~30–35% шанс).
- **Метод**: TCN ~360k параметров: последние 35 дней дневного разрешения + 26 недель недельного (эффективная T≈61 токен); ResBlocks Conv1d k=3 dil 1/2/4; календарные эмбеддинги на токен (DoW, день года, праздники 23.02/08.03) + статика в голову (lifetime-gmv, тенюр, средний интервал). Лосс MSE на z суммарного 30д-gmv. Аугментация случайными якорями с recency-bias (τ≈120д) → ~1.2M окон/эпоху. AdamW lr 3e-4 cosine, EMA, fp32 MPS; эпоха 1–2 мин, цикл 25–40 мин. Плотные матрицы [250k, 410, ~12] fp16 ≈ 2.5 ГБ — влезает.
- **Роль**: бленд `expm1(α·ẑ_NN+(1−α)·ẑ_LGBM)`, α∈{0.2..0.5} сеткой; если улучшения нет — отбросить без сожалений (sunk cost ≈ 40 мин).
- **Результат**: —
- **Вывод**: —

---

## Эксперимент 8 — Стекинг базовых моделей с грид-поиском гиперпараметров

- **Гипотеза**: ансамбль разнородных моделей (LightGBM, HistGBR, CatBoost, Ridge) в
  z-пространстве с мета-моделью на честных OOF-предиктах побьёт одиночный CatBoost exp01
  (1.70261); интерсепт мета-модели встроит глобальную калибровку уровня (exp06).
- **Метод** (`src/train_stack.py`): Stage A — компактные сетки по моделям на внутреннем
  сплите fold_00+01→fold_02; Stage B — OOF leave-one-fold-out по fold_00..02;
  Stage B2 — прод-бейзы на всех трёх фолдах (ES на fold_02); Stage C — RidgeCV/OLS(positive)
  на pooled OOF; fold_03 — чистый холдаут. Фичи: все блоки (base+x_*+pca32+btyd, 141 кол.).
- **Результат** (fold_03): стек OLS **1.67103**, ridgecv 1.67105; соло: cat 1.67046,
  hist 1.67230, lgbm 1.67291, ridge 1.71487. Веса OLS: cat 0.61 / lgbm 0.27 / hist 0.13 /
  ridge 0, intercept +0.0238. Лучшие параметры — в reports/stack_search.json.
- **Вывод**: против exp01 выигрыш −0.032 RMSLE (−1.9%). Стек не превзошёл лучший соло-модель
  на холдауте — GBDT сильно коррелированы; ценность стека — робастность на private и
  автоматическая калибровка уровня. Сабмиты: submissions/submission_stack_ols.csv,
  submission_bl_{lgbm,hist,cat}.csv (ridge-сабмит артефактно тяжёлохвостный — не отправлять).
  Отдельно: блочная абляция одиночным CatBoost с рефитом на все 4 фолда
(src/submit_baseline_blocks.py, reports/bl_blocks.json) дала base 1.649 → +ext 1.647 →
+pca 1.645 → +btyd 1.643 — это диагностические in-sample числа (fold_03 в трейне),
для сравнения блоков между собой, не для лидерборда.
Следующий шаг по плану — калибровка/пороги поверх стека (exp06-сетка) и hurdle-сборка (exp03).

<!-- Шаблон новой записи:
## Эксперимент N — <название>
- **Гипотеза**: ...
- **Метод**: ...
- **Результат**: ... (CV метрика по фолдам, время обучения)
- **Вывод**: ... (идём дальше / откатываем)
-->

---

# Часть 2. EDA сырых данных (reports/eda_2026_08_24.md)

# EDA train.parquet — 2026-08-24

Автоотчёт: `src/eda.py`. Назначение — проверка гипотез о структуре данных для приоритизации экспериментов 2–5.

## 1. Схема и целостность

колонки (18): ['event_date', 'user_id', 'search', 'cat', 'has_search_to_cart', 'has_search_to_ord', 'has_cat_to_cart', 'has_cat_to_ord', 'search_to_cart', 'search_to_ord', 'cat_to_cart', 'cat_to_ord', 'gmv_search', 'gmv_cat', 'to_cart', 'to_ord', 'gmv', 'searches']
строк: 30,631,006, юзеров: 250,000, даты: 2025-01-01 .. 2026-02-13
календарных дней в диапазоне: 409, плотность рядов: 30.0%
пропуски: 0 колонок с NaN
дубликаты (user_id, event_date): 0
полностью пустых дней в данных: 5575442 (18.20%) — прореженные ряды подтверждены
отрицательные значения минимумов: нет
несоответствия тождеств: {'gmv_split': 77370, 'ord_split': 0, 'cart_split': 0, 'flag_ord': 0, 'gmv_wo_ord': 0, 'ord_wo_gmv': 0}

## 2. Календарь: тренд и сезонность платформы

средний GMV платформы по дням недели (1=Пн): 1:0.7M, 2:0.7M, 3:0.7M, 4:0.7M, 5:0.6M, 6:0.6M, 7:0.7M
разброс DoW: min/max = 0.94
месячные агрегаты (GMV/млн, средние дневные активные юзеры/тыс, GMV/день на активного юзера):
  2025-01: gmv=   14.6M  act_users/day= 57.1k  gmv/user/day=     8
  2025-02: gmv=   14.5M  act_users/day= 61.8k  gmv/user/day=     8
  2025-03: gmv=   17.4M  act_users/day= 64.2k  gmv/user/day=     9
  2025-04: gmv=   17.0M  act_users/day= 64.9k  gmv/user/day=     9
  2025-05: gmv=   17.8M  act_users/day= 65.4k  gmv/user/day=     9
  2025-06: gmv=   18.7M  act_users/day= 67.1k  gmv/user/day=     9
  2025-07: gmv=   20.4M  act_users/day= 69.2k  gmv/user/day=    10
  2025-08: gmv=   22.6M  act_users/day= 73.8k  gmv/user/day=    10
  2025-09: gmv=   21.9M  act_users/day= 78.1k  gmv/user/day=     9
  2025-10: gmv=   23.5M  act_users/day= 84.5k  gmv/user/day=     9
  2025-11: gmv=   24.0M  act_users/day= 92.5k  gmv/user/day=     9
  2025-12: gmv=   28.8M  act_users/day= 95.8k  gmv/user/day=    10
  2026-01: gmv=   21.8M  act_users/day= 90.2k  gmv/user/day=     8
  2026-02: gmv=    9.1M  act_users/day= 93.9k  gmv/user/day=     7

близнец тестового окна [14.02–15.03.25]: 16.7M
смежное до [15.01–13.02.25]: 14.4M (index 1.163)
смежное после [16.03–14.04.25]: 16.5M (index 1.014)
окно перед сабмитом [15.01–13.02.26]: 21.0M (YoY к прошлому году: 1.460)

профиль близнецового окна 2025 (gmv дня / средняя база января):
  01.02:1.0 14.02:1.1 20.02:1.2 21.02:1.1 22.02:1.1 23.02:1.1 24.02:1.0 25.02:1.1 01.03:1.3 06.03:1.3 07.03:1.1 08.03:1.1 09.03:1.2 14.03:1.2 20.03:1.1 21.03:1.1 22.03:1.1 23.03:1.2 24.03:1.2 25.03:1.1

## 3. Пользователи: история и активность

активных дней на юзера, квантили 10/25/50/75/90: [ 26.  51. 102. 178. 254.] из 409
дней с заказами на юзера, квантили: [ 0.  3. 10. 26. 49.]
юзеров с >=1 заказом за весь период: 219,168 (87.7%)
юзеров, впервые появившихся после января 2025: 60,554 (24.2%)
общий GMV юзера, квантили 50/90/99: [ 388. 2764. 9680.]; топ-1% юзеров держит 14.9% всего GMV

## 4. Таргет по фолдам: структура, персистентность, наив

anchor     | zeros | mean z | sd z | p50/p90/p99 y       | naive RMSLE | corr(z_past,z_fut)
fold_00 2025-12-03| 42.4% |  2.51 | 2.39 | [  15.  289. 1279.] |  2.22630 | 0.560
fold_01 2025-12-17| 44.0% |  2.39 | 2.35 | [  12.  259. 1186.] |  2.21661 | 0.558
fold_02 2025-12-31| 46.4% |  2.23 | 2.29 | [   8.  217. 1045.] |  2.22673 | 0.557
fold_03 2026-01-14| 45.9% |  2.24 | 2.29 | [   8.  216. 1040.] |  2.19506 | 0.557
(в outer join нулевые юзеры = нет активности в окне => y=0)

## 5. Сегменты по рекенси на якоре fold_03 (2026-01-14)

сегмент                  | юзеров | доля GMV окна | mean y | zeros | naive RMSLE сегмента
заказ <7д                |  63923 |         57.0% |   187.2 | 16.3% | 2.097
заказ 8–30д              |  78782 |         31.6% |    84.1 | 33.6% | 2.528
заказ 31–90д             |  45181 |          8.3% |    38.6 | 56.3% | 2.550
заказ 91–180д            |  17409 |          1.6% |    18.7 | 73.8% | 1.870
заказ >180д / никогда    |  44705 |          1.6% |     7.7 | 88.6% | 1.211

## 6. Интент-сигналы: поиски/корзины без заказов -> будущий GMV

без заказов за 14д до якоря: 156,397
  c поисками/корзиной: 120,745 -> mean y = 42.4, P(y>0) = 41.8%
  без активности:      35,652 -> mean y = 23.8, P(y>0) = 26.3%

P(y>0) и mean y по децилям searches_14d:
  s14<=      0: n=57,058  P(y>0)=29.1%  mean y=   27.0
  s14<=      1: n=43,059  P(y>0)=40.5%  mean y=   40.3
  s14<=      4: n=55,402  P(y>0)=54.4%  mean y=   64.5
  s14<=     11: n=46,335  P(y>0)=68.2%  mean y=  103.0
  s14<=     24: n=48,146  P(y>0)=81.8%  mean y=  194.9

## 7. Хвост и форма z = log1p(y) для лосса

skew(z) = 0.35, kurtosis(z) = 1.66
квантили z: [0.   0.   2.19 4.27 6.95]
доля y>10000: 0.00%, y>50000: 0.000%

## 8. Follow-up: YoY одной когорты, семантика пустых дней, холодный старт

когорта юзеров, активных в обоих окнах [15.01–13.02]: 190,797 из 190,797 (2025)
per-user GMV: 75.4 -> 98.5 (YoY индекс на юзера = 1.307)
per-user активных дней: 9.3 -> 12.2
per-user GMV/активный день: 8.1 -> 8.1

пустые по конверсии дни: 5,575,442; из них с флагом визита: 18.4% (search=0.0%, cat=18.4%)
fold_end: без активности >=  7д:  48,769 (19.5%); без заказа >=  7д/никогда: 183,680 (73.5%)
fold_end: без активности >= 14д:  21,211 (8.5%); без заказа >= 14д/никогда: 153,083 (61.2%)
fold_end: без активности >= 30д:       0 (0.0%); без заказа >= 30д/никогда: 114,835 (45.9%)
fold_end: без активности >= 60д:       0 (0.0%); без заказа >= 60д/никогда:  78,810 (31.5%)

---

# Часть 3. Манифест фичевых артефактов (data/v2/MANIFEST.md)

# Манифест артефактов данных (handoff для обучения)

Дата: 2026-08-24. Автор: этап данных (EDA + трансформации). Обучение и сборка бейзлайна —
следующий этап. Все пути от корня репо. Окружение: `.venv` (polars, sklearn, scipy, catboost).

## Карта экспериментов → артефакты

| Эксперимент | Артефакт | Генератор |
|---|---|---|
| exp02 — расширенные фичи | `data/v2/features_ext/fold_*/batch_*.parquet` (блок `x_*`) | `src/build_features_ext_pca.py` |
| exp07 — PCA дневных панелей | тот же файл (блок `pca_00..31`) | там же |
| exp04 — BTYD фичи | `data/v2/features_bgnbd/<fold>.parquet` + `fit_params.json` | `src/build_features_bgnbd.py` |
| exp03 — hurdle приоры | `reports/exp03_hurdle_priors.json` | `src/exp03_hurdle_priors.py` |
| exp06 — калибровка уровня | `reports/exp06_calibration.json` | `src/exp06_calibration_indices.py` |

## Схема `features_ext` (250k строк × 133 колонки, по фолду fold_00..03 + fold_end)

- Ключи: `anchor_date`, `user_id`, таргет `target` (= сумма gmv за [anchor+1, anchor+30];
  в `fold_end` — NULL).
- **base (70 кол.)**: оконные sum/max/mean по gmv/searches/to_ord/to_cart за 7/14/30/60/90d,
  active_days_30d, recency_to_ord/searches_days (999 = никогда), tenure_days, order_days_total,
  row_days_total, conv-ratios 90d. NaN → 0 (паритет с exp01, кроме 14d окна — оно новое).
- **x_* (28 кол.)** — интент/EWMA/тренды/частота:
  интент: x_searches_sum_14d, x_cart_sum_14d, x_ord_sum_14d, x_gmv_sum_14d, x_cart_no_ord_days_14d,
  x_visit_only_days_30d (визиты каталога без действий), x_search_days_14d, x_intent_no_ord_14d;
  источники: x_gmv_search/cat_sum_30d, доли x_share_gmv_search/cat_30d, конверсии x_conv_s2o/c2o;
  EWMA (log-space): x_gmv/ord_ewma_h7/h30, моментумы x_ewma_momentum_gmv/ord;
  тренды: x_gmv_slope_56d, x_ord_slope_56d, x_gmv_share_14_of_30;
  частота: order_days_total, x_aov_30d, x_due_ratio.
  Ratio-фичи могут быть NaN (нулевой знаменатель) — CatBoost нативно; для других моделей заполнить.
- **pca_00..pca_31**: IncrementalPCA(32) поверх стандартизованной плотной панели
  [56 дней × log1p(gmv, searches, to_ord, to_cart)] = 224 dims → 32 компоненты (EVR = 0.514).
  Scaler+PCA фитились ТОЛЬКО на пре-якорных окнах train-фолдов fold_00..02 (утечки нет);
  компоненты для fold_03/fold_end — чистый transform.

## Схема `features_bgnbd` (по фолду, один parquet)

Ключи: `anchor_date`, `user_id`. Колонки: входы (bgnbd_T, bgnbd_tx, bgnbd_n_occasions,
bgnbd_mon_freq, bgnbd_mbar) + предикты: bgnbd_p_alive, bgnbd_en30 (BG/NBD hyp2f1-формула),
eb_lambda_n30 (Gamma-Poisson EB, устойчиво на холодных), gg_e_value (Gamma-Gamma шринкейдж чека),
произведения bgnbd_e_gmv30 / eb_e_gmv30.
Параметры MLE на pooled train-фолдах: r=1.251, α=19.2, a=0.135, b=1.238; GG: p=0.071, q=3.02,
v=1546 (⚠️ p<1 ⇒ веса почти всегда 0 → gg_e_value ≈ популяционному среднему 54; использовать
как слабую фичу/константу шринкейжа). ⚠️ В fold_00 отсутствуют юзеры, впервые появившиеся после
якоря (247,929 из 250k) — джойнить как left + fill_null или inner.

## Как собрать обучающую таблицу

```python
import polars as pl
feats = pl.read_parquet("data/v2/features_ext/fold_03/batch_*.parquet")
btyd  = pl.read_parquet("data/v2/features_bgnbd/fold_03.parquet")
df = feats.join(btyd, on=["anchor_date", "user_id"], how="left")
```

Train = concat(fold_00..02), val = fold_03, прод-инференс = fold_end. Лосс: RMSE на z=log1p(target),
финальный предикт expm1. Референсные точки fold_03: zero=3.20364, median=2.28900, naive=2.19506,
CatBoost(exp01)=1.70261.

## Калибровка после обучения (exp06)

`z' = z + c + β·log(s)`; индексы в reports/exp06_calibration.json:
s_seasonal=1.1628 (сезонный аплифт тестового окна), yoy_cohort_user=1.3067 (структурный рост),
local_drift=0.8094 (локальный даунтафт перед якорем сабмита — декабрьский пик нормализуется).
Сетки c/β — внутри JSON. Приоры хёрдла (P(buy), E[z|buy] по сегментам рекенси×интент) —
в reports/exp03_hurdle_priors.json.

## Воспроизведение

```bash
.venv/bin/python src/build_features_ext_pca.py      # ~4 мин
.venv/bin/python src/build_features_bgnbd.py        # ~1 мин
.venv/bin/python src/exp03_hurdle_priors.py         # ~1 мин
.venv/bin/python src/exp06_calibration_indices.py   # ~1 мин
```
